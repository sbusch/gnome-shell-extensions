Development nodes
-----------------

<alt>+<f2> lg -> looking glass console
<alt>+<f2> r  -> reload gnome shell (as of 02/2017 not available in wayland under fedora 25)

journalctl /usr/bin/gnome-shell -f -o cat
(older version: journalctl /usr/bin/gnome-session -f -o cat)

use in combination with log('xyz')
log() is a shorthand for global.log()
